import { AsyncStorage } from 'react-native';

export default class DataStore {
    static updatePlayLists(list){
        for(playlist in list){
            
        }
    }

    static updateSongs(songsList)
}

